<h1> <%= @service.name %> </h1>

<p>What I do:  <%= @service.description %></p> 
<p>I'm poor, gimme:  <%= number_to_currency(@service.price, :precision => 2)%></p>

<p><%= image_tag(@service.picture, :class => 'picture')%></p>

<p><%= @service.text %></p>

<% if current_seller  %>
<%= link_to "Edit this service!", edit_service_path(@service) %>
<%= link_to "Delete this service!", :service, method: :delete, data: { confirm: "Are you sure you want to delete this service?"} %>
<%end%>
<%= link_to "Back to the service list!", services_path %>