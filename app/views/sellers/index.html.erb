<h1>Home Page</h1>

<% flash.each do |key,value|  %>
    <div class="<%= key %>" > <%= value %> </div>
<% end %>

<%= link_to "Register", new_seller_path  %>

<% if current_seller  %>
  <%= link_to "Logout", destroy_session_path %>
<% else %>
    <%= link_to "Login", new_session_path %>
<% end %>

<p><%= link_to "To services list", services_path%></p>
